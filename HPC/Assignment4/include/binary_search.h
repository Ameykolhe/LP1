#ifndef BINARY_SEARCH
#define BINARY_SEARCH

#include <vector>

using namespace std;

bool binary_search_serial(vector<int> arr, int key);

bool binary_search_parallel(vector<int> arr, int key);

#endif